<Page
    x:Class="Assignment_UWP_Full.Pages.AccountForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Assignment_UWP_Full.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Pivot Background="{ThemeResource SystemControlAccentAcrylicWindowAccentMediumHighBrush}">
            <Pivot.TitleTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="Account" Style="{ThemeResource HeaderTextBlockStyle}" Foreground="White"/>
                    </Grid>
                </DataTemplate>
            </Pivot.TitleTemplate>

            <PivotItem Header="Login" Margin="0">
                <Frame  x:Name="Login"/>

            </PivotItem>
            <!--<PivotItem Header="My Information" Margin="0">
                <Frame  x:Name="MyInfo"/>

            </PivotItem>-->
            <PivotItem Header="Register" Margin="0">
                <Frame  x:Name="Register"/>
            </PivotItem>

        </Pivot>
    </Grid>
</Page>
